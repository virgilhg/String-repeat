// Solution is incomplete. Challenge was time restrited, with more time, I'd account for faster run time and use a built in method(i.e repeat)
function repeatStr (n, s) {
  let str = ""
  for(let i=0; i=n; i++){
     str += s
  }
  return str;
}
